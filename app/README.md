# IronVibe Telegram (Flutter web)

Исходники миниаппа. Корень репозитория — на уровень выше: [README](../README.md).

```bash
flutter pub get
flutter run -d chrome
flutter build web --release --base-href /
```
